# 完整环境配置清单（基于你现有本地资源整理）
> 前置现状：
> PostgreSQL：Windows本地实例 localhost:5432
> Redis：Windows Redis 8.4.0 本地 6379
> 知乎开放平台 AccessKey：`2673bc5a0352dd783ebbffcb6fd790c17b312c23`
> 运行载体：Windows，.NET Web服务 + Redis Celery任务队列架构

## 一、PostgreSQL 配置（区分 Staging / Production，两套独立实例）
### 1）Staging（测试环境，本地开发）
- DATABASE_URL
```
postgresql://postgres:04017736@localhost:5432/staging_company?sslmode=disable
```
- 连接参数
  - 主机：`localhost`
  - 端口：`5432`
  - 账号：`postgres`
  - 密码：`04017736`
  - 数据库名：`staging_company`
  - sslmode：`disable`（本地无SSL）
- 权限：账号拥有 **DDL+DML完整权限**，支持alembic/migration执行建表、修改字段、索引迁移
- 备份恢复方式
  - 备份：`pg_dump -U postgres -d staging_company -F c -f staging_backup_$(date).dump`
  - 恢复：`pg_restore -U postgres -d staging_company backup.dump`



## 二、Redis 配置（Windows Redis 8.4.0 localhost:6379）
### 连接URL
```ini
# Celery Broker（任务队列）
CELERY_BROKER_URL = redis://localhost:6379/0
# Celery 任务结果存储
CELERY_RESULT_BACKEND = redis://localhost:6379/0
# 业务缓存地址
CACHE_REDIS_URL = redis://localhost:6379/1
```
> 建议拆分db：db0=celery队列，db1=业务缓存

### Redis 能力说明
1. TLS：**当前Windows本地实例未开启TLS**；公网部署必须开启TLS加密
2. ACL：当前使用默认账号，无ACL权限隔离；生产环境创建独立ACL账号限制命令
3. 连接上限：日志提示系统ulimit限制最大客户端 `3168`，受Windows系统文件句柄限制
4. 内存策略：建议配置 `maxmemory-policy allkeys-lru`，内存达到阈值自动淘汰缓存key

### Redis日志关键提醒
```
maxclients has been reduced to 3168 to compensate for low ulimit
```
Windows环境天然存在句柄限制，如果爬虫并发量大，需要拆分多Redis实例或者调优并发worker数量。



## 四、数据源访问授权策略
### 1. 允许访问域名清单
> 填入各家企业官网域名，白名单控制爬虫目标

### 2. 允许的ATS招聘平台清单
示例清单（可扩充）
`Greenhouse, Lever, Workday, BambooHR, Ashby, JazzHR`

### 3. 调用限速规范
- 单域名 QPS：≤2
- 单日单域名最大请求：5000次
- 条款：仅抓取**公开招聘职位信息**，禁止爬取员工个人信息；遵循robots.txt

### 4. 渲染策略
✅ **允许浏览器渲染（Playwright/Selenium）**，应对JS动态渲染招聘页面
❌ 禁止无限制高频无头浏览器抓取，增加随机请求间隔

### 5. 禁止访问
- 登录才可见的内网招聘系统
- 明确标注禁止爬虫、有反爬协议的平台

## 五、模型服务（当前暂无，预留配置模板）
```ini
DASHSCOPE_API_KEY:sk-a5f9cf21d76b424d9f7024b47309b74b	
```
限制项预留：
- 每分钟请求限额、Token上限
- 日/月度预算上限
- 数据策略：是否发送原始网页内容至第三方大模型、数据是否出境、隐私脱敏规则

## 六、知乎数据源配置（你当前可用Token）
```ini
ZHIHU_ACCESS_SECRET = "2673bc5a0352dd783ebbffcb6fd790c17b312c23"
```
### 授权与配额说明
1. 渠道：知乎数据开放平台 **试用Token**
2. 用途约束：**仅限公司发现、企业关联证据挖掘，不作为完整职位数据源**
3. 限制信息（平台页面说明）
   - 所有Token共享统一试用额度，额度耗尽全部失效
   - 无固定公开QPS阈值，建议自我限流 QPS ≤1
   - 无IP白名单（试用版特性；正式商用可申请IP白名单）
   - Token特性：一旦关闭弹窗，密钥无法二次查看；丢失只能删除重建
4. 过期规则：试用额度耗尽即不可用，无固定时间过期，额度消耗为准

> 重要提醒：不要高频调用知乎接口，避免额度快速耗尽；优先缓存结果到PostgreSQL
）
- 操作系统：Windows
- 应用载体：原生进程运行（.NET Web服务），**尚未使用Docker/K8s**
- 中间件：Windows版 Redis 8.4.0、本地PostgreSQL
- 网络：本机localhost，无公网域名、无HTTPS

# 补充执行建议
1. 先在PostgreSQL新建 `staging_company` 数据库
2. Redis区分db，隔离队列和缓存，防止数据互相污染


